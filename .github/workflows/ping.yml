name: Keep MBU Bot Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl wake endpoint
        run: |
          curl -fsS https://mbu-tradingbot.onrender.com/wake || true
